[package]
name = "nn-reftest"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Reference tensor comparison tooling for nn — verified ML framework"

[lints]
workspace = true

[features]
default = ["nn-core"]
nn-core = ["dep:nn-core"]
spectral = ["dep:rustfft"]

[dependencies]
nn-core = { workspace = true, optional = true }
safetensors = { workspace = true }
thiserror = { workspace = true }
half = { workspace = true }
rustfft = { workspace = true, optional = true }

[dev-dependencies]
nn-dsl = { workspace = true }
serde_json = { workspace = true }
