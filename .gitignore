# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
/env/
/venv/
/.venv/

# Rust
target/
target_*/
Cargo.lock
**/*.rs.bk
.ruff_cache

# C++
*.o
*.out
*.exe
build/

# Sync artifacts

# File freshness tracking (hooks)

# Epic state tracking (Tech Lead long-horizon coordination)
.epic_state/

# Shared coordination (per-machine state)
.ait/

# Pulse runtime artifacts
metrics/
metrics
.metrics/
.flags
.ownership_conflicts.log
logs/

# Benchmark outputs
benchmarks/**/results/
benchmarks/**/logs/
benchmarks/**/data/

# Eval outputs
evals/**/results/
evals/**/logs/

# Emacs
*~
\#*\#
.#*

# Test/lint output
.coverage
.coverage.*
.vision_health_cache/
htmlcov/
.pytest_cache/
.mypy_cache/
.jscpd-report/

# Credentials and secrets
.env
.env.*
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets.json
*_secret*
*.secret
.secrets/

# === Project-specific (preserved during sync) ===
# Add project-specific gitignore entries below this line.
# They will be preserved when sync_repo.sh updates this file.
.hypothesis

# MCP plugin artifacts
.playwright-mcp/

# Test scheduler runtime state (per-machine, gitignored)
.test_state.json
.test_state.json.lock

# Mutation testing artifacts
mutants/
.mutmut-cache/

# Generated by design_lifecycle --write-index
designs/index.json
designs/INDEX.md
# Scoped test index
.scoped_test_index.json
# Model weights (generated by converter scripts, not committed)
models/
weights/
# Build artifacts (rustc direct output)
rust_out
# Migration backup (#2577)
nn_verify_status.json.bak
# Generated bound analysis reports (#2461)
nn_kokoro_bound_analysis_report.json
# Generated proof certificates
*.proof.json
# ...except the committed Silero VAD bundle the proof-bundle tests require (#1680).
!silero_vad.proof.json
# Local config directory (but track the nextest test policy below)
.config/*
# ...track the nextest test policy (OOM-prevention; see CLAUDE.md)
!.config/nextest.toml
# PyTorch reference tensors (regenerate: python3 scripts/generate_references.py)
test_data/references/

# Test output artifact (regenerated by kokoro_pass_impact_production test)
kokoro_optimizer_summary.json
# Generated verification status artifacts (history grows per run; do not commit)
nn_verify_status_*.json
